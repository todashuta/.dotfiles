# screenrc...

#==============================
#== キーバインドに関する設定 ==
#==============================
# escapeキー変更 C-j
escape ^Jj

# ESCAPE-xのスクリーンロックの無効化
bind ^x

#==================
#== その他の設定 ==
#==================
# 起動メッセージオフ
startup_message off

# 自動デタッチをオンにする
autodetach on

# 保存するバッファを1000行にする
defscrollback 1000

#==========================
#== ステータスライン関連 ==
#==========================
# 最下行の上と分割部に表示される部分の設定
caption always "%{= gw}%{gW}%-w%{= wk}%n %t%{-}%+w %=%{gw} $USER@$HOSTNAME[$LANG] %{= kw} ESC:<C-j>"
# 最下行の常に表示される部分の設定
hardstatus alwayslastline "%{=b Wk} GNU Screen %{= gw} %=%{= gw} (%l) %Y-%m-%d %02c:%s"

# end of file
