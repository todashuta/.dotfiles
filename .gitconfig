# .gitconfig
# https://github.com/todashuta/profiles

[color]
	# colorの設定(自動で色付け)
	status = auto
	diff = auto
	branch = auto
	interactive = auto
	grep = auto
[alias]
	cm = commit -m
	ss = status -sb
	sl = shortlog -sn
	difff = diff --word-diff

[push]
	default = simple

# end of .gitconfig
